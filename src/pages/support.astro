---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import SupportBlock from '../components/SupportBlock.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="ru">
	<head>
		<BaseHead title={`${SITE_TITLE} - Поддержка`} description="Поддержите автора и продолжение выхода дайджестов" />
	</head>
	<body>
		<Header />
		<main class="support-page">
			<div class="support-content">
				<div class="support-hero">
					<h1 class="support-hero__title">Поддержка проекта</h1>
					<p class="support-hero__subtitle">
						Frontend Weekly — это бесплатный еженедельный дайджест самых важных новостей из мира фронтенд-разработки
					</p>
				</div>

				<div class="support-info">
					<div class="support-info__card">
						<h2 class="support-info__heading">Зачем поддерживать?</h2>
						<ul class="support-info__list">
							<li>Регулярные еженедельные выпуски с самыми важными новостями</li>
							<li>Кураторская работа — отбор только актуального и полезного контента</li>
							<li>Экономия вашего времени на поиск информации</li>
							<li>Развитие русскоязычного комьюнити фронтенд-разработчиков</li>
						</ul>
					</div>

					<div class="support-info__card support-info__card--accent">
						<h2 class="support-info__heading">Что дает поддержка</h2>
						<ul class="support-info__list">
							<li>Возможность продолжать выпускать качественные дайджесты</li>
							<li>Мотивация на создание нового контента</li>
							<li>Улучшение качества материалов</li>
							<li>Ваше имя в списке благодарностей (опционально)</li>
						</ul>
					</div>
				</div>

				<div class="support-block-container">
					<SupportBlock />
				</div>
			</div>
		</main>
		<Footer />
	</body>
</html>

<style>
	.support-page {
		padding: var(--space-3xl) var(--space-md);
		min-height: 70vh;
	}

	.support-content {
		max-width: var(--content-width);
		margin: 0 auto;
	}

	.support-hero {
		text-align: center;
		margin-bottom: var(--space-4xl);
		padding-bottom: var(--space-3xl);
		border-bottom: 4px solid rgb(var(--black));
		position: relative;
	}

	.support-hero::after {
		content: '';
		position: absolute;
		bottom: -4px;
		left: 50%;
		transform: translateX(-50%);
		width: 120px;
		height: 4px;
		background: var(--accent);
	}

	.support-hero__title {
		font-size: var(--text-4xl);
		font-weight: 900;
		margin: 0 0 var(--space-lg) 0;
		letter-spacing: -0.04em;
		line-height: 1;
	}

	.support-hero__subtitle {
		font-size: var(--text-lg);
		line-height: 1.6;
		color: rgb(var(--gray-dark));
		max-width: 60ch;
		margin: 0 auto;
	}

	.support-info {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: var(--space-2xl);
		margin-bottom: var(--space-4xl);
	}

	.support-info__card {
		padding: var(--space-2xl);
		background: rgb(var(--white));
		border: 3px solid rgb(var(--black));
		box-shadow: 6px 6px 0 rgb(var(--black));
		transition: all var(--transition-base);
	}

	.support-info__card:hover {
		transform: translate(3px, 3px);
		box-shadow: 3px 3px 0 rgb(var(--black));
	}

	.support-info__card--accent {
		background: rgba(255, 51, 102, 0.05);
		border-color: var(--accent);
		box-shadow: 6px 6px 0 var(--accent);
	}

	.support-info__card--accent:hover {
		box-shadow: 3px 3px 0 var(--accent);
	}

	.support-info__heading {
		font-size: var(--text-2xl);
		font-weight: 800;
		margin: 0 0 var(--space-lg) 0;
		letter-spacing: -0.02em;
	}

	.support-info__list {
		list-style: none;
		margin: 0;
		padding: 0;
	}

	.support-info__list li {
		position: relative;
		padding-left: var(--space-xl);
		margin-bottom: var(--space-md);
		font-size: var(--text-base);
		line-height: 1.6;
	}

	.support-info__list li::before {
		content: '→';
		position: absolute;
		left: 0;
		color: var(--accent);
		font-weight: 900;
		font-size: var(--text-lg);
	}

	.support-block-container {
		margin-top: var(--space-4xl);
	}

	@media (max-width: 768px) {
		.support-page {
			padding: var(--space-2xl) var(--space-md);
		}

		.support-hero__title {
			font-size: var(--text-3xl);
		}

		.support-info {
			grid-template-columns: 1fr;
			gap: var(--space-lg);
		}

		.support-info__card {
			padding: var(--space-xl);
		}
	}
</style>
