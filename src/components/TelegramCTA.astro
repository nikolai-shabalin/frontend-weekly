<div class="telegram-cta">
  <div class="telegram-cta__header">
    <div class="telegram-cta__icon">
      <svg viewBox="0 0 24 24" aria-hidden="true" width="40" height="40">
        <path fill="currentColor" d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.562 8.248-1.97 9.341c-.145.658-.537.818-1.084.508l-3-2.21-1.446 1.394c-.14.18-.357.223-.535.223l.19-2.72 5.56-5.023c.232-.21-.05-.327-.358-.118l-6.871 4.326-2.962-.924c-.643-.204-.657-.643.136-.953l11.57-4.461c.535-.197 1.004.128.832.953z"/>
      </svg>
    </div>
    <div class="telegram-cta__content">
      <h3 class="telegram-cta__title">Присоединяйтесь к Telegram-каналу</h3>
      <p class="telegram-cta__description">
        Получайте уведомления о новых дайджестах, обсуждайте новости с коллегами и делитесь своим опытом в дружелюбном сообществе фронтенд-разработчиков
      </p>
    </div>
  </div>
  <a 
    href="https://t.me/greatAttractorCode" 
    target="_blank" 
    class="telegram-cta__button" 
    rel="noopener noreferrer"
  >
    <span class="telegram-cta__button-text">Подписаться на канал</span>
    <span class="telegram-cta__button-arrow">→</span>
  </a>
</div>

<style>
  .telegram-cta {
    margin: var(--space-3xl) 0;
    padding: var(--space-2xl);
    background: rgb(var(--white));
    border: 3px solid rgb(var(--black));
    box-shadow: var(--shadow-brutal);
    position: relative;
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
  }

  .telegram-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 6px;
    background: linear-gradient(90deg, #0088cc 0%, #00aaff 100%);
  }

  .telegram-cta__header {
    display: flex;
    gap: var(--space-lg);
    align-items: flex-start;
  }

  .telegram-cta__icon {
    flex-shrink: 0;
    color: #0088cc;
    padding: var(--space-md);
    background: rgba(0, 136, 204, 0.1);
    border: 2px solid #0088cc;
    transition: all var(--transition-base);
  }

  .telegram-cta:hover .telegram-cta__icon {
    transform: rotate(-10deg) scale(1.05);
  }

  .telegram-cta__content {
    flex: 1;
  }

  .telegram-cta__title {
    font-size: var(--text-xl);
    font-weight: 800;
    margin: 0 0 var(--space-sm) 0;
    color: rgb(var(--black));
    letter-spacing: -0.02em;
  }

  .telegram-cta__description {
    margin: 0;
    color: rgb(var(--gray-dark));
    font-size: var(--text-sm);
    line-height: 1.6;
  }

  .telegram-cta__button {
    display: inline-flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-md) var(--space-2xl);
    background: #0088cc;
    color: rgb(var(--white));
    border: 3px solid rgb(var(--black));
    box-shadow: 4px 4px 0 rgb(var(--black));
    font-size: var(--text-base);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    text-decoration: none;
    transition: all var(--transition-fast);
    align-self: flex-start;
  }

  .telegram-cta__button:hover {
    background: #00aaff;
    transform: translate(2px, 2px);
    box-shadow: 2px 2px 0 rgb(var(--black));
  }

  .telegram-cta__button-arrow {
    display: inline-block;
    transition: transform var(--transition-base);
  }

  .telegram-cta__button:hover .telegram-cta__button-arrow {
    transform: translateX(4px);
  }

  @media (max-width: 768px) {
    .telegram-cta {
      padding: var(--space-xl);
    }

    .telegram-cta__header {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .telegram-cta__button {
      width: 100%;
      justify-content: center;
      font-size: var(--text-sm);
    }
  }
</style>
