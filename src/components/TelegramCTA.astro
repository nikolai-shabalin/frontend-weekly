<div class="telegram">
  <div class="telegram__icon">
    <svg viewBox="0 0 24 24" aria-hidden="true" width="32" height="32">
      <path fill="currentColor" d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.562 8.248-1.97 9.341c-.145.658-.537.818-1.084.508l-3-2.21-1.446 1.394c-.14.18-.357.223-.535.223l.19-2.72 5.56-5.023c.232-.21-.05-.327-.358-.118l-6.871 4.326-2.962-.924c-.643-.204-.657-.643.136-.953l11.57-4.461c.535-.197 1.004.128.832.953z"/>
    </svg>
  </div>
  <div class="telegram__content">
    <div class="telegram__text">
      <h3 class="telegram__title">Присоединяйтесь Telegram-каналу</h3>
      <p class="telegram__description">Получайте уведомления о новых дайджестах, обсуждайте новости с коллегами и делитесь своим опытом в дружелюбном сообществе фронтенд-разработчиков</p>
    </div>
    <a href="https://t.me/greatAttractorCode" target="_blank" class="telegram__button" rel="noopener noreferrer">
      Подписаться
    </a>
  </div>
</div>

<style>
  .telegram {
    margin: var(--space-2xl) 0;
    padding: var(--space-xl);
    background:
      radial-gradient(circle at 8% 12%, rgba(var(--accent-yellow-bg-rgb), 0.35), transparent 56%),
      var(--paper-color);
    border-radius: var(--radius-md);
    border: 2px solid rgba(var(--line), 0.5);
    box-shadow: 0 8px 16px -6px rgba(var(--accent-rgb), 0.15);
    position: relative;
  }

  .telegram__icon {
    position: absolute;
    top: var(--space-md);
    right: var(--space-md);
    color: var(--accent);
    opacity: 0.8;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% {
      transform: scale(1);
      opacity: 0.8;
    }
    50% {
      transform: scale(1.1);
      opacity: 1;
    }
  }

  .telegram__content {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  .telegram__text {
    flex: 1;
  }

  .telegram__text .telegram__title {
    margin: 0 0 var(--space-xs);
    font-size: var(--text-xl);
    color: var(--accent);
  }

  .telegram__description {
    margin: 0;
    color: rgb(var(--ink-soft));
    font-size: var(--text-sm);
    line-height: 1.5;
  }

  .telegram__button {
    display: inline-flex;
    align-items: center;
    width: fit-content;
    justify-content: center;
    padding: var(--space-sm) var(--space-lg);
    background-color: var(--accent);
    color: white;
    text-decoration: none;
    border-radius: 999px 999px 999px 0;
    font-weight: 700;
    transition: transform 0.2s ease, background-color 0.2s ease;
  }

  .telegram__button:hover {
    transform: translateY(-2px);
    background-color: var(--accent-dark);
  }

  @media (max-width: 640px) {
    .telegram {
      margin: var(--space-xl) 0;
    }

    .telegram__text {
      text-align: center;
    }

    .telegram__button {
      width: 100%;
      max-width: 100%;
    }
  }
</style>
